bundle:
  name: speakorp

resources:
  apps:
    app:
      name: "speakorp"
      description: "Barebones hello world Databricks App"
      source_code_path: ./

      # Start the app automatically on deploy.
      lifecycle:
        started: true
      # Uncomment to enable on behalf of user API scopes. Available scopes: sql, dashboards.genie, files.files, serving.serving-endpoints
      # user_api_scopes:
      #   - sql

targets:
  default:
    default: true
    workspace:
      host: https://dbc-ccb985ab-2291.cloud.databricks.com
